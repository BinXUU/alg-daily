#include <iostream>
#include <stdio.h>
#include <string.h>
#include <vector>
#include <algorithm>
#include <queue>

using namespace std;

typedef long long ll;
typedef unsigned long long ull;
typedef long double ld;

const int M = 1e4 + 10;
const int mod = 1e9 + 7;

string bfs(){
    int dx[4]={-1,0,1,0},dy[4]={0,1,0,-1};
    char op[4]={'u', 'r', 'd', 'l'};

    
}

//八数码问题无解，当且仅当 逆序对数量为奇数
//1~8每个数和最终位置的曼哈顿距离之和
//string来存状态
int main(){
    ios::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    
    system("pause");
    return 0;
}
